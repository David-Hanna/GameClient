#ifndef PLAYERSERVER_H
#define PLAYERSERVER_H

#include <QObject>

class PlayerServer : public QObject
{
    Q_OBJECT
public:
    explicit PlayerServer(QObject *parent = 0);

signals:

public slots:

};

#endif // PLAYERSERVER_H
